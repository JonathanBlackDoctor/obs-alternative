<Application x:Class="SilentStream.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnExplicitShutdown">
    <!-- No StartupUri: this is a background host. The status box / control UI are
         created on demand. Composition happens in App.OnStartup. -->
    <Application.Resources>
        <!-- 다크 테마 (계획서 §3.8) -->
        <SolidColorBrush x:Key="BgBrush" Color="#1E1E24" />
        <SolidColorBrush x:Key="PanelBrush" Color="#27272F" />
        <SolidColorBrush x:Key="FgBrush" Color="#E8E8EC" />
        <SolidColorBrush x:Key="DimBrush" Color="#8A8A95" />
        <SolidColorBrush x:Key="AccentBrush" Color="#3D8BFD" />

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource FgBrush}" />
        </Style>
        <Style x:Key="MetricText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource FgBrush}" />
            <Setter Property="FontFamily" Value="Consolas" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Margin" Value="10,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="{StaticResource FgBrush}" />
            <Setter Property="Background" Value="{StaticResource PanelBrush}" />
            <Setter Property="BorderBrush" Value="#3A3A44" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="10,6" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource PanelBrush}" />
            <Setter Property="Foreground" Value="{StaticResource FgBrush}" />
            <Setter Property="BorderBrush" Value="#3A3A44" />
            <Setter Property="Padding" Value="4,3" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="#1E1E24" />
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#15151A" />
            <Setter Property="Foreground" Value="#C8C8D0" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource FgBrush}" />
        </Style>
        <Style TargetType="Slider">
            <Setter Property="Margin" Value="0,4,0,0" />
        </Style>
    </Application.Resources>
</Application>
